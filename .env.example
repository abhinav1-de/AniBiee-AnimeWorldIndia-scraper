# Server Configuration
PORT=3000
NODE_ENV=development

# CORS Configuration
# Set to '*' to allow all origins, or specify comma-separated list of allowed origins
# Example: CORS_ORIGIN=https://example.com,https://another.com
# Example: CORS_ORIGIN=https://anizen.tr
CORS_ORIGIN=*

# API Base URL (optional, defaults to http://localhost:3000)
# API_BASE_URL=http://localhost:3000
